/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'images.pexels.com',
      'picsum.photos',
    ],
  },
}

module.exports = nextConfig
